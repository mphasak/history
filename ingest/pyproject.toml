[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "history-sim-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1",
    "psycopg[binary]>=3.1",
]
